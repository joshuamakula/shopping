var cart = ["Hp EliteBook", "Dell", "Viao", "Toshiba"];

for(let item = 0; item <= cart.length; item++){
    // console.log(cart[item]);
}

// Add to cart
/* cart.push("Dell Celeron");
console.log(cart);
 */
// Remove from cart
cart.splice(0,1)
console.log(cart);
